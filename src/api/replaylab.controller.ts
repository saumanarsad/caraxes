import { Controller } from '@nestjs/common';

@Controller('replaylab/api')
export class ReplayLabController {
  // TODO: GET /cases — list all captured cases
  // TODO: GET /cases/:id — get a single case with curl export
  // TODO: POST /cases/:id/diff — submit replayed response and return diff
}
